services:
  - type: web
    name: docwise-api
    runtime: node
    rootDir: backend
    buildCommand: npm ci && npm run build:deploy
    startCommand: npm run start:deploy
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: 'false'
