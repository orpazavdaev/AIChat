services:
  - type: web
    name: docwise-api
    runtime: node
    rootDir: backend
    buildCommand: npm ci --include=dev && npm run build:deploy
    startCommand: npm run start:deploy
    envVars:
      - key: NODE_ENV
        value: production
